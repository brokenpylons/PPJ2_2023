 Aeggito:= AOkii;

for (faeSh := HtiSit to 1) begin
       CONSOLE 1;
   MAitit :=10 * 0;
    for ( A := 123 to MreOE ) begin
  for ( AtSCR := 00 + COLS to 1 ) begin
   CONSOLE ArCCl
            end;
  AOgOc := COLSE;
    CONSOLE COLu;
    MtSCc :=00
    end;
    
MidgRte:= AAdC;
 i :=WikS;
                 for (HreOE:=02 to 1 * Higth) begin
 MiRiito:= iOkSi;
  faeCE := faeg;
 
for ( M := faiiEteta - 0 to seg + iAdCito ) begin
MiSOlt:= FtRtEleta * 12 & MCgSi * 1
end
end
       end;
  
CONSOLE AASCi
         